

const InitialState = {
    Product: [],
    backupProduct: []
}


const reducer = (state = InitialState, actions) => {
    switch (actions.type) {

        default: {
            return { ...state }
        }
    }
}

export default reducer;